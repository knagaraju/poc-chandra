import { AdjustRowHeightDirective } from './adjust-row-height.directive';

describe('AdjustRowHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new AdjustRowHeightDirective();
    expect(directive).toBeTruthy();
  });
});
